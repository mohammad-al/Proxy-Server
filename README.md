#Setup
It is recommended to use Firefox. To setup the server, navigate to Firefox settings, and under General, select Network Settings. Select the manual proxy configuration, and set the HTTP Proxy to 127.0.0.1 and the port to 65000.
Now, you can safely start the server by running python3 proxy.py.
